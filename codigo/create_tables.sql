create table Produto_Estoque(
    produto varchar,
    estoque int
);

create table reserva(
    nome varchar,
    dia_hora varchar
);

create table ponto(
    horario timestamp,
    funcionario_nome varchar
);
